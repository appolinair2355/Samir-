services:
  - type: worker
    name: telegram-card-bot
    env: python
    buildCommand: "pip install -r render_requirements.txt"
    startCommand: "python main.py"
    plan: free
    region: frankfurt
    envVars:
      - key: API_ID
        sync: false
      - key: API_HASH
        sync: false
      - key: BOT_TOKEN
        sync: false
      - key: ADMIN_ID
        sync: false
